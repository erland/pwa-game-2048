# 2048 game as a PWA

    Minimal Phaser + Vite starter wired to `@erlandlindmark/pwa-game-2d-framework`.

    ## Setup

    ```bash
    pnpm install
    pnpm dev
    ```

    Edit scenes under `src/scenes/*` and the Phaser config in `src/main.ts`.
